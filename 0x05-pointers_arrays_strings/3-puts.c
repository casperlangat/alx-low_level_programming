#include "main.h"

/**
 * _puts - prints a string, followed by a new line.
 * @str: input string.
 * Return: no return.
 */

void _puts(char *str)
{
int i;

i = 0;
while (s[i] != '\0')
{
i++
}
for (i = i - 1; i >= ; i--)
{
_putchar(s[i]);
}
_putchar('\n');
}
